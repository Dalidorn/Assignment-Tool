export default function AssignmentManagement({ params }) {
    return (
        <div>
            <h1>Assignment Management: {params.assignmentName}</h1>
            <ul>
                <li>Buttons for options</li>
                <li>To handle different tasks</li>
                <li>Related to the assignment in question</li>
            </ul>
        </div>
    );
};